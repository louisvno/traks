import { MapDirective } from './map.directive';

describe('MapDirective', () => {
  it('should create an instance', () => {
    const directive = new MapDirective(null, null);
    expect(directive).toBeTruthy();
  });
});
